{
  enable = true;
  ln.connectionType = {{ connection_type }};
  logLevel = {{ log_level }};
  generateDotEnvFile = {{ generate_env_file }};
  dotEnvFile = {{ env_file }};
  passwordFile = {{ password_file }};
  rootPath = {{ root_path }};
  nginx = {
    enable = {{ nginx_enable }};
    openFirewall = {{ nginx_open_firewall }};
    location = {{ nginx_location }};
  };
}
